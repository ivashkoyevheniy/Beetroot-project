import React from "react";
import { AppLayout } from "../../components";

export const Settings = () => {
    return (
        <AppLayout>
        <div className="in-dev">Current page in development progress &#61;)</div>
        </AppLayout>
    );
}